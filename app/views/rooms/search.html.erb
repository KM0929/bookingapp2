<div class="container-fluid room">
  <h4><%= "検索結果：#{@numbers}件" %></h4>
  <div class="row room-row">
    <div class="room-information">
      <% @rooms.each do |room| %>
        <div class="room-frame-inner">
            <%= link_to controller: 'rooms', action: 'show', id: room.id do %>
                <div class="room-image-out">
                  <%= image_tag room.room_image.url, :alt => 'ルーム画像', class: "frame-room-image" %>
                  <div class="room-profile">
                    <p class="room-profile-name">
                      <%= room.room_name %>
                    </p>
                    <p class="room-profile-city">
                      <%= room.room_address %>
                    </p>
                    <p class="room-profile-price">
                      ¥<%= room.room_price %>
                    </p>
                  </div>
                </div>
            <% end %>
        </div>
      <% end %>
    </div>
  </div>
  
</div>